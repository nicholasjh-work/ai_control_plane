services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: ai_control_plane_api
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
